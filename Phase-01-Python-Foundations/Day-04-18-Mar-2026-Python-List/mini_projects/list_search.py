# List Element Search
# Problem: Write a Python program to check if a given element exists in a list.

numbers = [10, 20, 30, 40, 50]
element = 30
if element in numbers:
    print(f"{element} exists in the list.")
else:
    print(f"{element} does not exist in the list.")
